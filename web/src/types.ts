export type PackageInfo = {
    packageName: string,
    name: string,
}
export type ModuleInfo = {
    moduleDir: string
    description: string
    id: string
    action: string
    enabled: string
    update: string
    remove: string
    updateJson: string
    author: string
    versionCode: string
    web: string
    mount: string
    name: string
    version: string
}